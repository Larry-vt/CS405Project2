import java.util.ArrayList;

public class Main {
	public FileReader() {
		ArrayList<Process> all = new ArrayList<Process>();//every process that'll run in order
		for(int i = 0; i< 4;i++) {
			//goes through and reads every 
			//mem_max value
			//proc max value
			//etc 
		}
		for(int i =0; i<proc_maxvalue;i++) {
			//random size proc_max_Size
			//random for time < max_proc_time
			 Process p = new Process();// with variables
			 all.add(p);
			 
		}
		
	}

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		MemoryAllocator ma = new MemoryAllocator(Memory_MAX,ArrayList<Process> all);
		
	}

}
